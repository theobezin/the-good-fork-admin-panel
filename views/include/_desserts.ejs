<% for(var i = 0;i < desserts.length; i++){ %>

    <tr>
        <td><%= i+1 %></td>
        <td><img width="100px"  src=<%= desserts[i].picture_url %> ></td>
        <td><%= desserts[i].name %></td>
        <td><p><%= desserts[i].price %> €</p></td>
        
        <td>
            <a href="/update-dessert?id=<%= desserts[i]._id %>" class="btn border-shadow update">
                <span class="text-gradient"><i class="fas fa-pencil-alt"></i></span>
            </a>
            <a  class="btn border-shadow delete" data-id=<%= desserts[i]._id %> >
                <span class="text-gradient"><i class="fas fa-times"></i></span>
            </a>
        </td>
    
        
    </tr>
    
    
    
    <% } %>